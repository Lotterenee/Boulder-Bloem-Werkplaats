{
  "name": "harness-railway-starter",
  "private": true,
  "version": "0.0.0",
  "description": "Delete-me starter that proves the harness Railway pipeline is live. Replace with your real app.",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
